<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.egemenozdag.reserveit0001">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".Reservations" />
        <activity android:name=".createRestaurantAccount" />
        <activity android:name=".ApprovalRequests" />
        <activity android:name=".RestaurantPage" />
        <activity android:name=".UserProfile" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateCustomerAccountActivity" />
        <activity android:name=".restaurantProfile" />
        <activity android:name=".createRestaurant" />
        <activity android:name=".editDesign" />
        <activity android:name=".SpareTable" />
        <activity
            android:name=".findingRestaurant"
            android:label="@string/title_activity_kullanici_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".adminProfile" />
    </application>

</manifest>